// src/theme/spacing.js
// Plain numbers only — React Native has no units. Measure the repeating
// spacing scale in Figma Dev Mode and adjust these if they don't match.

export const spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
  xxl: 48,
};

// Common radii, since RN also takes plain numbers for borderRadius
export const radius = {
  sm: 8,
  md: 16,
  lg: 24,
  pill: 999, // fully rounded buttons
};
